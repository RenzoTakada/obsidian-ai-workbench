# Prompt: Implement with Claude Code

Read this repository and implement the Obsidian AI Workbench pattern in my Obsidian vault.

Requirements:

1. Ask me for the vault path if it is not obvious.
2. Create `_AI/` inside the vault.
3. Copy/adapt `templates/generic/_AI/`.
4. Add `CLAUDE.md` based on `templates/claude-code/CLAUDE.md`.
5. Do not read or modify notes outside `_AI/` unless I explicitly authorize it.
6. Do not delete anything.
7. Explain the final structure and how I should use it.
8. If Ollama is installed, optionally help configure embeddings/search, but do not install software without asking.
