# MEMORY.md — OpenClaw Workbench Memory

This is the memory index for the `_OpenClaw/` workbench.

The parent vault is human-authored space.

Other agent folders such as `_Claude/` and `_Codex/` are separate workbenches and should not be accessed unless explicitly authorized.
