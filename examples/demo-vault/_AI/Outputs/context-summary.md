# Context Summary

Status: draft

The demo vault shows how a terminal AI agent can load durable memory from `Memory/MEMORY.md`, recover short-lived context from `Memory/hot.md`, and keep generated work isolated inside `_AI/`.

This output has not been promoted to permanent notes.
