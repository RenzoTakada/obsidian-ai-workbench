# CLAUDE.md — Demo Workbench

Load `Memory/MEMORY.md`, linked memory files, and `Memory/hot.md` before responding.

Keep generated work inside `_AI/` unless the human explicitly authorizes otherwise.

Available command intents:

- `/brain`
- `/context`
- `/save`
- `/review-memory`
- `/spec`
