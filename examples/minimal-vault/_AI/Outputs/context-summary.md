# Context Summary

The demo vault shows a terminal AI workbench isolated inside `_AI/`.

The agent should load `Memory/MEMORY.md`, follow linked memory files, keep generated work in `_AI/Outputs/`, and ask before touching human-authored notes outside the workbench.

This output is a draft. It can be promoted to permanent notes only after human review.
